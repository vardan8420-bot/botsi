# 🚀 Инструкция по деплою на Railway

## ✅ Что уже сделано:
- Весь код готов и исправлен
- Все 4 этапа реализованы
- railway.toml настроен для PostgreSQL
- Исправлен конфликт имен переменных

## 📋 Что нужно сделать:

### 1. Добавить переменные окружения в Railway

Зайди в Railway Dashboard → Variables и добавь:

**Обязательные:**
```
TELEGRAM_BOT_TOKEN=твой_токен_от_BotFather
OPENAI_API_KEY=твой_openai_ключ
DATABASE_URL=postgresql://neondb_owner:npg_aYFLPEk35XlM@ep-square-sunset-adn5dr29-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require
```

**Опциональные (можно добавить позже):**
```
CACHE_ENABLED=true
CACHE_TTL=3600
MAX_CONTEXT_MESSAGES=5
GPT4O_PROBABILITY=0.05
```

**Для соцсетей (если нужно):**
```
INSTAGRAM_USERNAME=твой_username
INSTAGRAM_PASSWORD=твой_password
YOUTUBE_API_KEY=твой_youtube_key
TIKTOK_SESSION_ID=твой_tiktok_session
FACEBOOK_ACCESS_TOKEN=твой_facebook_token
GITHUB_TOKEN=твой_github_token
```

### 2. После добавления переменных

Railway автоматически:
1. Обнаружит изменения
2. Установит PostgreSQL через railway.toml
3. Установит зависимости
4. Запустит бота

### 3. Проверь логи

Должно быть:
```
✅ Конфигурация валидна
✅ База данных подключена
✅ AI обработчик инициализирован
✅ Генератор контента инициализирован
⚠️ Соцсети: нет доступных платформ (добавьте API ключи)
✅ Аналитика инициализирована
✅ AI разработчик инициализирован
⚠️ GitHub: токен не найден (добавьте GITHUB_TOKEN)
✅ Бот успешно запущен!
```

## 🎯 Когда бот заработает?

Сразу после того как:
1. Добавишь 3 обязательные переменные (TELEGRAM_BOT_TOKEN, OPENAI_API_KEY, DATABASE_URL)
2. Railway завершит деплой (2-3 минуты)

## ✅ Что будет работать сразу:

### Этап 1 - Основное:
- Обычные сообщения с AI
- Голосовые сообщения (Whisper)
- Армянский + русский + английский
- Транслит → армянский
- `/help`, `/language`, `/stats`, `/reset`

### Этап 2 - Генерация контента:
- `/generate_blog` - статьи
- `/generate_post` - посты для соцсетей
- `/generate_script` - сценарии видео
- `/generate_ad` - рекламные тексты
- `/social_status` - статус соцсетей

### Этап 3 - Аналитика:
- `/analytics` - глобальная статистика
- `/activity` - активность
- `/top_users` - топ пользователей
- `/model_stats` - статистика моделей
- `/cache_stats` - эффективность кеша
- `/export_data` - экспорт данных
- `/language_stats` - распределение по языкам

### Этап 4 - AI Разработчик:
- `/generate_code` - генерация кода
- `/analyze_code` - анализ кода
- `/fix_code` - исправление багов
- `/explain_code` - объяснение кода
- `/refactor_code` - рефакторинг
- `/generate_tests` - генерация тестов
- `/github_status` - статус GitHub

## 🔥 ИТОГО: 19 команд + основной функционал!
